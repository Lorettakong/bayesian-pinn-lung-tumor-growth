# Public Release Notes

This cleaned repository was exported from the original working project to make GitHub publication easier.

Cleanup actions:

- kept manuscript code and manuscript-facing outputs
- removed local environment and IDE state
- removed bulky intermediate result folders and checkpoints
- kept preprocessing utilities in a separate folder for provenance
- omitted earlier metadata tables that only supported local preprocessing

No algorithm logic was changed during this cleanup.
